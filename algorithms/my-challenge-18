function notDivisible(arr) {
    let newArr = [];
    for(let i = 0; i < arr.length; i++) {
        if(arr[i] % 5 != 0) {
            newArr.push(arr[i]);
        }
    }
    return newArr;
}

console.log(notDivisible([3,5,15]))
console.log(notDivisible([23, 12]))
console.log(notDivisible([3,25]))